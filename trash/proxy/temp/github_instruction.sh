git init
git config --global user.email "k5shao@ucsd.edu"
git config --global user.name "SmartestKen"
git remote add origin https://github.com/SmartestKen/SiteInfo.git
git remote rm origin
git pull origin master


git rm -r --cached /folder -q
git add 
git commit -m "..."
# use -u if the only branch is master
git push origin master
